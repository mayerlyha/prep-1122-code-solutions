const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log(languages);

const firstElement = languages;
console.log('The first element of the array is', firstElement[0]);

const thirdElement = languages;
console.log('The 3rd element of the array is', thirdElement[2]);

var length = languages.length;
console.log('The length of the array is', length);

const lastElement = languages;
console.log('The last element of the array is', lastElement[languages.length - 1]);
